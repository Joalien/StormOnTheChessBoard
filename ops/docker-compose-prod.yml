services:

  backend:
    build:
      context: ..
      dockerfile: ops/Dockerfile.backend

  nginx:
    build:
      context: ..
      dockerfile: ops/Dockerfile.frontend
    ports:
      - "${SOTC_PORT:-8080}:8080"
    depends_on:
      - backend
