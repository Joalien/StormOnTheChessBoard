import card.Card;
import card.LightweightSquadCard;
import org.junit.jupiter.api.Test;
import state.GameStateController;

import java.lang.reflect.InvocationTargetException;
import java.util.List;

import static org.junit.jupiter.api.Assertions.assertEquals;

class ChessBoardAggregateTest {
    @Test
    void should_return_valid_card() throws ClassNotFoundException, InvocationTargetException, InstantiationException, IllegalAccessException {
//        GameStateController gameStateController = new GameStateController();
//        gameStateController.startGame();
//        command.PlayCardCommand playCardCommand = command.PlayCardCommand.builder()
//                .cardName("card.LightweightSquadCard")
//                .parameters(List.of("d2", "e2")).build();
//
//        Card card = new Controller().createCardFromJson(gameStateController, playCardCommand);
//
//        assertEquals(LightweightSquadCard.class, card.getClass());
    }
}